# Optional: set if signup/login get "Backend unavailable" (proxy not reaching backend).
# Then API requests go directly to the backend. Leave empty to use Vite proxy.
# VITE_API_ORIGIN=http://localhost:5001
